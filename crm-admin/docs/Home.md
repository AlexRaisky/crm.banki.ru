---
tags: [moc, home, index]
---

# Home

**crm-admin** — самописная админ-панель управления CRM-коммуникациями, заменившая low-code-админку на Appsmith.
Один артефакт Java 21 / Spring Boot 3.3 (`crm-admin.jar`) со встроенным vanilla-JS SPA поверх PostgreSQL 16, в Docker, тремя контурами (prod / preprod / test) из одного образа.
Панель ведёт шаблоны коммуникаций семи каналов (sms, push, email, cc, fa, vk, la — данные пока по первым четырём) и синхронизирует их с прод-БД, собирает цепочки коммуникаций и материализует их в конфигурацию прод-движков, а также обслуживает планирование промо, отчёты и клиентские инструменты команды.

Читать первым — [[Флоу системы]] (сквозной рассказ «как всё работает»), держать под рукой — [[Шпаргалка]].

## Архитектура

- [[Обзор архитектуры]] — как связаны части: компоненты, сквозные потоки данных, ключевые дизайн-решения.
- [[Технологии]] — стек и версии: бэкенд, БД, фронтенд, инфраструктура.
- [[Среды и деплой]] — три контура, compose-стеки, переменные окружения, серверная раскатка и скрипты.
- [[Безопасность и RBAC]] — вход, роли-данные, матрица прав, разделы, супер-админ, серверные проверки.

## Бэкенд

- [[Обзор бэкенда]] — структура пакетов `ru.banki.crm`, контроллеры, сервисы, сквозные паттерны.
- [[Шаблоны и мастер коммуникаций]] — CRUD шаблонов в едином справочнике, генерация кодов, батч-цепочка дней.
- [[Синхронизация с прод-БД]] — очередь доставки во внешний прод, ETL «прод → мы», сверка.
- [[Цепочки (Journeys)]] — хранение схем цепочек в jsonb и CRUD `/api/journeys`.
- [[Материализация (Flow)]] — превращение схемы цепочки в строки слоёв A и B.
- [[Аудит и журналирование]] — прикладной журнал `arch.t_admin_log` (триггерный `arch.arch_log` больше не наполняется).
- [[Конфигурация]] — `application.yml`, профили, все переменные окружения.

## Фронтенд

- [[Обзор фронтенда]] — SPA без сборки: состав файлов, бутстрап, клиентские инструменты.
- [[Оболочка панели (shell)]] — App Launcher, сайдбар и flyout, обзорные страницы, тема и язык, ACL-контракт NAV.
- [[Мастер коммуникаций (формы)]] — формы каналов, автоимена, цепочка дней, карточка просмотра настроек.
- [[Flow Builder (цепочки)]] — канва Drawflow: типы узлов, модалка настроек, предпросмотр и материализация.
- [[Управление доступом и вход]] — страница входа и раздел управления пользователями и ролями в `/settings`.

## Разделы панели

- [[Отчёты]] — встраивание отчётов Tableau и выгрузка «ЧЕК СМС траффик» в Excel.
- [[Планирование промо]] — общая таблица плана промо и правила совместного редактирования.

## База данных

- [[Схема БД]] — все схемы, миграции и сиды, ER-связи, аудит на уровне БД.
- [[Таблицы приложения]] — собственные таблицы панели: пользователи и роли, цепочки, промо, настройки, очередь синка.
- [[Справочники шаблонов]] — единый справочник `template.d_template` и внешние канальные прод-таблицы.
- [[Слой A (flow)]] — нормализованная модель процесса коммуникаций.
- [[Слой B (процессные таблицы)]] — копии прод-таблиц, которые читают боевые движки.

## API

- [[REST API]] — сводка всех эндпоинтов `/api/**` с телами запросов, кодами ошибок и правилами доступа; Swagger UI на `/swagger-ui.html`.

## Сквозное

- [[Флоу системы]] — путь данных от входа пользователя до прод-движков, со схемами.
- [[Шпаргалка]] — быстрые команды и пути для эксплуатации и разбора проблем.
