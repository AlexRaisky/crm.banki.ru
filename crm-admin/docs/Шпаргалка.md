---
tags: [cheatsheet, ops]
---

# Шпаргалка

Только команды и пути. Объяснения — по ссылкам: [[Флоу системы]] · [[Среды и деплой]] · [[Безопасность и RBAC]] · [[Синхронизация с прод-БД]] · [[Материализация (Flow)]] · [[Схема БД]] · [[REST API]].

## Контуры

| Контур | Адрес | Приложение | БД |
|---|---|---|---|
| prod | домен через nginx :80 | `crm-admin-app-prod` | `crm-admin-db-prod` :5432 |
| preprod | 127.0.0.1:8081 | `crm-admin-app-preprod` | `crm-admin-db-preprod` :5433 |
| test | 127.0.0.1:8082 | `crm-admin-app-test` | `crm-admin-db-test` :5434 |

```bash
ssh -L 8081:127.0.0.1:8081 -L 8082:127.0.0.1:8082 user@сервер   # UI preprod/test
ssh -L 15432:127.0.0.1:5432 user@сервер                          # БД прод в DBeaver: localhost:15432 / crm / crm
```

## Обновление сервера

```bash
cd crm.banki.ru/crm-admin && git pull                      # ветка admin-panel
docker compose -f docker-compose.yml -f docker-compose.server.yml build app-prod
docker compose -f docker-compose.yml -f docker-compose.server.yml \
  up -d --no-deps --force-recreate app-prod app-preprod
docker compose -f docker-compose.yml -f docker-compose.server.yml up -d --no-deps app-test

./scripts/reset-test.sh          # сброс test с нуля
./scripts/refresh-preprod.sh     # preprod = копия прода
```

## Диагностика

```bash
docker compose ps
docker logs crm-admin-app-prod --tail 50
docker restart crm-admin-nginx
curl -s http://localhost/nginx-health

# очередь синка
docker exec crm-admin-db-prod psql -U crm -d crm -c \
 "select id, channel, operation, local_code, prod_code, status, attempts, left(coalesce(last_error,''),200) from app.prod_sync order by id desc limit 10;"
# перезапустить упавшие / очистить
docker exec crm-admin-db-prod psql -U crm -d crm -c \
 "update app.prod_sync set status='PENDING', attempts=0, last_error=null where status='ERROR';"
docker exec crm-admin-db-prod psql -U crm -d crm -c "delete from app.prod_sync;"

# метки ETL
docker exec crm-admin-db-prod psql -U crm -d crm -c "select * from app.sync_watermark;"
# последние действия пользователей
docker exec crm-admin-db-prod psql -U crm -d crm -c \
 "select timestamp_cr, action_user, table_name, operation from arch.t_admin_log order by id desc limit 15;"
# роли и их права
docker exec crm-admin-db-prod psql -U crm -d crm -c \
 "select r.name, rs.section_id, rs.can_read, rs.can_add, rs.can_edit, rs.can_delete from app.role r join app.role_section rs on rs.role_id=r.id order by r.sort_order, rs.section_id;"
# шаблоны
docker exec crm-admin-db-prod psql -U crm -d crm -c \
 "select channel, code, communication_name, campaign_name, active_flag from template.d_template order by channel, code;"

# связь с внешним продом напрямую
docker run --rm -e PGPASSWORD='пароль' postgres:16 psql -h ХОСТ -p ПОРТ -U ЮЗЕР -d БАЗА -c "select 1;"

# бэкап
docker exec crm-admin-db-prod pg_dump -U crm crm | gzip > backup_$(date +%F).sql.gz
```

Первый экран при проблемах с продом: `/settings` → **Диагностика**.

## Пути: бэкенд

`src/main/java/ru/banki/crm/`

| Что | Класс |
|---|---|
| Хранилище шаблонов | `service/TemplateStore` |
| Бизнес-слой шаблонов | `service/TemplateService` |
| Правила каналов, очередь синка | `service/UnifiedTemplateService` |
| Справочники форм | `service/DictionaryService` |
| Генерация имён | `service/NamingService` |
| Внешняя прод-БД | `service/prod/ProdDbService` |
| Доставка очереди | `service/prod/ProdSyncService` |
| ETL «прод → мы» | `service/prod/NoticeEtlService` |
| Сверка с продом | `service/prod/ProdReconcileService` |
| Материализация цепочек | `service/flow/MaterializationService` |
| Хранение цепочек | `service/DbJourneyService` / `service/MockJourneyService` |
| План промо | `service/PromoPlanService` |
| Отчёт «ЧЕК СМС траффик» | `service/SmsCheckReportService` |
| Реестр подключений к БД | `service/DbConnectionService` |
| Пользователи | `service/UserService` |
| Роли и матрица прав | `service/RoleService` |
| Список разделов RBAC | `service/Sections` |
| Проверка прав | `security/AccessGuard` |
| Вход, куки, URL-правила | `security/SecurityConfig` |
| Authorities по флагам роли | `security/AppUserPrincipal` |
| Супер-админ на старте | `config/AdminBootstrap` |
| Журнал действий | `service/AdminLogService` |
| GUC для `arch.arch_log` | `service/AuditContext` |

## Пути: фронтенд

`src/main/resources/static/`

| Файл | Что там |
|---|---|
| `index.html` | вся разметка: шапка, сайдбар, обзорные страницы, секции `sec-*` / `view-*`, диалоги |
| `css/shell.css` | переменные, темы, шапка, сайдбар |
| `css/section-*.css` | стили разделов (`@scope (#sec-…)`) |
| `js/shell.js` | `NAV`, `ICONS`, `renderNav`, `openSection`, `applyTheme`, `applyLang`, `t()`, `store`, App Launcher, «Общие параметры» |
| `js/wizard-forms.js` | формы мастера, подсказки полей, формулы имён |
| `js/template-details.js` | карточка «Просмотр настроек» |
| `js/template-list.js` | «Список шаблонов» |
| `js/dashboard.js` | дашборд |
| `js/promo.js` | «Планирование промо» |
| `js/reports.js`, `js/smscheck.js` | «Отчёты» и выгрузка «ЧЕК СМС траффик» |
| `js/onelink.js`, `js/srcbuilder.js` | OneLink Builder, Конструктор source |
| `js/heatmap-{data,core,io,ui}.js` | тепловая карта |
| `js/boot.js` | старт SPA (замыкает список `<script>`, порядок менять нельзя) |
| `api.js` | `CRM.*`, `applyNavAcl`, `syncAppSections` |
| `wizard.js`, `journeys.js`, `admin-users.js`, `combobox.js` | мастер, Flow Builder, управление доступом, combobox |
| `settings/index.html` | настроечная админка `/settings` |
| `login.html` | страница входа |

## Миграции

`src/main/resources/db/migration/` (сиды — `db/seed/`, V900+)

| | |
|---|---|
| V1 | схема шаблонов (канальные таблицы; удалены в V12) |
| V2 | пользователи и вход |
| V3 | цепочки `app.journeys` |
| V4 | `arch.t_admin_log` |
| V5 | слой B |
| V6 | слой A |
| V7 | `app.panel_settings` |
| V8 | единый справочник `template.d_template` + очередь `app.prod_sync` |
| V9 | очистка очереди |
| V10 | справочники форм (`dictionary.*`) |
| V11 | роль супер-админа |
| V12 | удаление локальных копий канальных таблиц (`notice`, `callcenter`) |
| V13 | каналы fa / vk / la |
| V14 | реестр подключений `app.db_connection` |
| V15 | флаг приёмника синка `is_prod_sync` |
| V16 | водяные знаки ETL `app.sync_watermark` |
| V17 | чистка прод-меток из `channel_props` |
| V18 | `app.promo_plan` |
| V19 | справочник `dictionary.d_product_type` |
| V20 | гранулярные права (read/add/edit/delete) |
| V21 | **роли как данные**: `app.role` + `app.role_section` |
| V22 | роль «Админ» + 5 разделов-просмотра |
| V23 | Маркетолог — 5 разделов только на чтение |
| V24 | `promo_plan.communication_name` |

## Частые правки — куда идти

| Задача | Где |
|---|---|
| Поля формы, значения по умолчанию | `index.html` + `wizard.js` |
| Формулы `communication_name` / `campaign_name` | `js/wizard-forms.js`, в карточке — `js/template-details.js` |
| Подсказки к полям мастера | `js/wizard-forms.js` (`FIELD_HELP_TEXTS`) |
| Стили раздела | `css/section-<раздел>.css`; общее — `css/shell.css` |
| Правила кодов, обязательные поля прода | `service/UnifiedTemplateService` |
| Что уезжает в прод | `service/TemplateStore.prodPayload` |
| Как строится INSERT в прод | `service/prod/ProdDbService.insert` |
| Пополнить справочники | `insert into dictionary.d_touch_point (value) values ('…');` |
| Разделы RBAC | `service/Sections` + `NAV` в `js/shell.js` + `SECTION_LABELS` в `admin-users.js` |
| Роли и права | `/settings` → «Управление доступом» (в БД — `app.role`, `app.role_section`) |
| Правила входа и URL-доступа | `security/SecurityConfig` |
| Узлы Flow Builder | `journeys.js` (`NODE_TYPES`) + `service/flow/MaterializationService` |

## Конфигурация (`.env`)

| Переменная | |
|---|---|
| `DB_NAME`, `DB_USER`, `DB_PASSWORD`, `DB_URL` | БД контура |
| `ADMIN_EMAIL`, `ADMIN_PASSWORD` | супер-админ |
| `APP_EMAIL_DOMAIN` | домен почты (пусто = `banki.ru`) |
| `PROD_DB_URL[_PREPROD\|_TEST]`, `PROD_DB_USER`, `PROD_DB_PASSWORD`, `PROD_DB_CONNECT_TIMEOUT_MS` | внешняя прод-БД |
| `ETL_ENABLED`, `ETL_INTERVAL_MS`, `ETL_FULL_CRON` | ETL «прод → мы» |
| `REMEMBER_ME_KEY`, `REMEMBER_ME_DAYS`, `SESSION_TIMEOUT` | сессии |
| `SESSION_COOKIE`, `REMEMBER_COOKIE` | имена кук (свои на контур) |
| `APP_BIND`, `DB_BIND` | на сервере `127.0.0.1` |
| `APP_ENV`, `FEATURES_DEV`, `JOURNEYS_MOCK` | имя контура, dev-фичи, хранилище цепочек |
