-- Планирование промо: отдельный столбец «Название коммуникации».
--
-- В нашей модели название коммуникации собирается автоматически (партнёр + продукт +
-- уникальное имя + канал, правила Конструктора source). Но при импорте календаря из
-- Excel название приходит готовым текстом («Альфа-Карта», «ОСАГО от Ренессанс»), а
-- партнёра/уникального имени в выгрузке нет. Храним его в отдельной колонке; грид
-- показывает сохранённое название, когда авто-сборка неполная.
ALTER TABLE app.promo_plan
    ADD COLUMN IF NOT EXISTS communication_name text NOT NULL DEFAULT '';
