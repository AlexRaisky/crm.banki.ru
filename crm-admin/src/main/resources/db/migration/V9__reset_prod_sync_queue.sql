-- Очистка очереди синка от записей, накопленных до подключения прод-БД.
-- Эти строки создавались во время отладки, когда PROD_DB_URL ещё не был задан:
-- доставлять их некуда, а «висящий» PENDING вводит в заблуждение.
-- Начиная с этой версии очередь наполняется ТОЛЬКО при настроенном подключении
-- (см. UnifiedTemplateService.enqueueProdSync).
DELETE FROM app.prod_sync;
