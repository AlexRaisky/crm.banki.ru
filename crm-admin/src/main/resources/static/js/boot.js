/* ---------- старт (после загрузки скриптов всех разделов) ----------

   Раздел больше не восстанавливается из localStorage: экран задаёт адрес.
   Голый адрес открывает Главную, глубокая ссылка — свой экран. Разбором и
   ожиданием готовности данных занимается маршрутизатор (js/router.js),
   поэтому здесь остался только запуск.

   Router.start() идёт последним намеренно: к этому моменту shell.js уже
   настроил адаптер, а модули разделов зарегистрировали свои провайдеры. */
renderGrid();
if (window.Router) Router.start();
if (UI_LANG === "en") applyLang(true);   /* полный перевод статики и перерисовка на выбранном языке */
